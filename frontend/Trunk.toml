[[hooks]]
stage = "pre_build"
command = "sh"
command_arguments = ["-c", "echo 'Building WASM frontend...'"]

[build]
target = "index.html"
dist = "dist"

[watch]
ignore = ["dist"]

[serve]
port = 8080
address = "0.0.0.0"
